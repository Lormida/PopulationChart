/**
 * Global types that don't high coupling in the application
 */

declare global {
  // type UnixTime = number
  // type CurrencyCode = 'USD' | 'RUB' | 'EUR'
  // type TIdSelector<T extends string> = T extends `${infer F}${infer Rest}` ? (F extends '#' ? string : never) : never
}
export {}
